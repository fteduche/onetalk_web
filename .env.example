# Example environment variables for local development
# Set your Formspree form ID if you want client-side form submissions.
# Get a form ID after creating a form at https://formspree.io
VITE_FORMSPREE_FORM_ID=your_formspree_form_id_here

# (Optional) Example allowed host that you might add to Vite's server.allowedHosts
# This project sets `allowedHosts: 'all'` in `vite.config.ts` for previews.
# If you prefer stricter settings, set allowedHosts in `vite.config.ts` to an array, e.g.: ['onetalk-web.onrender.com']
# VITE_ALLOWED_HOST=onetalk-web.onrender.com
